<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- action window -->
        <record id="action_fakultas" model="ir.actions.act_window">
            <field name="name">Fakultas</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{"search_default_fieldname":1}</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Fakultas
                </p><p>
                Click the Create button to add a new Fakultas
                </p>
            </field>
        </record>

        <menuitem id="menu_fakultas" name="Fakultas" parent="vit_akademik_config_menu" action="action_fakultas" sequence="40"/>
    </data>
</odoo>