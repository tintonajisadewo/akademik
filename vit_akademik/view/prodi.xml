<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- action window -->
        <record id="action_prodi" model="ir.actions.act_window">
            <field name="name">Prodi</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{"search_default_fieldname":1}</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                Click to add a new Prodi
                </p><p>
                Click the Create button to add a new Prodi
                </p>
            </field>
        </record>

        <menuitem id="menu_prodi" name="Prodi" parent="vit_akademik_config_menu" action="action_prodi" sequence="50"/>
    </data>
</odoo>