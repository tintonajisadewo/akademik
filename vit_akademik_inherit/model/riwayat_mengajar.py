#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError, Warning

class riwayat_mengajar(models.Model):
    _name = "vit.riwayat_mengajar"
    _inherit = "vit.riwayat_mengajar"
